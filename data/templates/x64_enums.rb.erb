require 'ffi'

module Evoasm
  module Libevoasm
<%= features.to_ruby_ffi %>
<%= inst_flags.to_ruby_ffi %>
<%= exceptions.to_ruby_ffi %>
<%= reg_types.to_ruby_ffi  %>
<%= reg_names.to_ruby_ffi %>
<%= param_names.to_ruby_ffi %>
<%= operand_types.to_ruby_ffi %>
<%= disp_sizes.to_ruby_ffi %>
<%= addr_sizes.to_ruby_ffi %>

    enum :x64_inst_id, [
<% insts.each do |inst| %>
      :<%= inst_name_to_ruby_ffi inst %>, <%= inst.index %>,
<% end %>
      :n_insts
    ]

    enum :x64_operand_size, [
      :'1',
      :'8',
      :'16',
      :'32',
      :'64',
      :'128',
      :'256',
      :'512',
      :n_operand_sizes
    ]
  end
end
